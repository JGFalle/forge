"""Batch (`--bulk`) processing for FORGE.

`discovery.py` scans the Drive "Que" folder, `lifecycle.py` does folder-lifecycle
path planning and crash-safe placement, and `orchestrator.py` wires those to
`pipeline.core.process_jd`. `report.py` renders the run summary.
"""
